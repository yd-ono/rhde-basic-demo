---
- name: Deploy application on microshift
  hosts: all
  tasks:
    - name: MicroShiftサービスの状態をStartedであることを確認する
      ansible.builtin.systemd:
        name: microshift.service
        state: started
      become: yes